// Calculate the area and perimeter of a rectangle
// _ area = width * length;
// _ perimeter = (width + length) * 2;

const width = 6;
const length = 4;

const area = width * length;
const perimeter = (width + length) * 2;

console.log(`Area: ${area}\nPerimeter: ${perimeter}`);
