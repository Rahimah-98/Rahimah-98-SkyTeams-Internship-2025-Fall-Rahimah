// Find the largest and smallest of three numbers

var a = 4;
var b = 2;
var c = 8;

var largest = Math.max(a, b, c);
var smallest = Math.min(a, b, c);

console.log(
  `Between these numbers:(${a}, ${b}, ${c}) the largest is: ${largest} and the smallest is ${smallest}`
);
